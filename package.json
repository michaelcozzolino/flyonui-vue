{
    "private": true,
    "workspaces": [
        "packages/core",
        "packages/docs"
    ],
    "packageManager": "yarn@4.6.0",
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "dev": "yarn concurrently \"yarn workspace flyonui-vue dev\" \"yarn workspace flyonui-vue-docs dev\" \"yarn move:build-size-visualizer\"",
        "build": "yarn workspaces foreach -Apt run build && yarn move:build-size-visualizer",
        "test": "yarn workspaces foreach -Apt run test",
        "audit": "yarn npm audit --all",
        "move:build-size-visualizer": "mv packages/core/build-size-visualizer.html packages/docs/public/build-size-visualizer.html"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
