{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "packageManager": "yarn@4.6.0",
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "dependency-upgrade": "npx yarn-upgrade-all",
        "dev": "yarn concurrently \"yarn workspace flyonui-vue dev\" \"yarn workspace flyonui-vue-docs dev\"",
        "build": "yarn workspaces foreach -Apt run build",
        "lint": "yarn workspaces foreach -Apt run lint",
        "test": "vitest run",
        "test-coverage": "vitest run --coverage",
        "audit": "yarn npm audit --all"
    },
    "devDependencies": {
        "@michaelcozzolino/eslint-config": "^1.1.2",
        "@vitest/coverage-v8": "^3.0.5",
        "concurrently": "^9.1.2",
        "eslint": "^9.23.0",
        "vitest": "^3.0.5"
    }
}
