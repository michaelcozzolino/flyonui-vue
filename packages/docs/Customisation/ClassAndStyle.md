# Classes and styles

Although, this is not encouraged, in some cases you might need some specific customizations that can be used without
changing the theme directly.

In the following example we will style an input component with a custom color and a custom size by using three different
approaches.

## Class to component

Apply the custom classes directly to the component

<ClassAndStyleDocs section="class-to-component" />

## Style to component

Use the style attribute to style the outest wrapper contained in the component

<ClassAndStyleDocs section="style-to-component" />

## Wrapper to component

Use the class attribute on top of an external wrapper to the component

<ClassAndStyleDocs section="wrapper-to-component" />
