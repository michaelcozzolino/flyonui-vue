# Text Direction (LTR | RTL)

The text direction is by default `LTR` and can be set in two ways:

## All components

By using the configuration you can set the same direction for all the `flyonui-vue` components.

<TextDirectionDocs section="all-components" />

## Single component

You can wrap the component into a container element such as `<div>` and use the `dir` attribute on it.

<TextDirectionDocs section="single-component" />
